<%@ page language="java" contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"%>

<div class="rating">
	<input type="radio" name="${param.id}" id="${param.id}-note-5" value="5"><label for="${param.id}-note-5">★</label>
	<input type="radio" name="${param.id}" id="${param.id}-note-4" value="4"><label for="${param.id}-note-4">★</label>
	<input type="radio" name="${param.id}" id="${param.id}-note-3" value="3"><label for="${param.id}-note-3">★</label>
	<input type="radio" name="${param.id}" id="${param.id}-note-2" value="2"><label for="${param.id}-note-2">★</label>
	<input type="radio" name="${param.id}" id="${param.id}-note-1" value="1"><label for="${param.id}-note-1">★</label>
</div>